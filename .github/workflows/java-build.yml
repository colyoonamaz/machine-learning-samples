name: Java CI

on: [push]
defaults:
  run:
    working-directory: targeted-marketing-java
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Set up JDK 7
        uses: actions/setup-java@v3
        with:
          java-version: '7'
          distribution: 'temurin'
      - name: Build with Maven
        run: mvn --batch-mode --update-snapshots package
